{
  "name": "sentinel-wallet-security",
  "version": "0.1.0",
  "private": true,
  "description": "Free, non-custodial wallet-security tool: revoke risky token approvals and detect/reset malicious EIP-7702 delegations.",
  "type": "module",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "web": "node src/web/server.ts",
    "scan": "node src/cli/scan.ts",
    "scan:live": "node src/cli/scan.ts --live",
    "test": "node --test src/**/*.test.ts"
  },
  "keywords": ["ethereum", "security", "approvals", "revoke", "eip-7702", "wallet"],
  "license": "MIT"
}
